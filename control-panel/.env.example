# Control Panel configuration
# Run `npm run dev:auto` from this directory (or `npm run setup-ip` from the
# repo root) to auto-detect your local IP and populate this file automatically.
# Manual overrides are still supported.

# Full URL of the backend server (default: http://localhost:3000)
VITE_SERVER_URL=http://localhost:3000

# Port the Vite dev server listens on (default: 5173)
VITE_PORT=5173
