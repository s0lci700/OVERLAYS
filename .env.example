# Server configuration
# Run `npm run setup-ip` (or `npm start`) to auto-detect your local IP and
# populate this file automatically.  Manual overrides are still supported.

# Port the server listens on (default: 3000)
PORT=3000

# Origin of the Svelte control panel (used for CORS; default: http://localhost:5173)
CONTROL_PANEL_ORIGIN=http://localhost:5173

# Character seed template in /data (file name without extension, default: template-characters)
CHARACTERS_TEMPLATE=template-characters
